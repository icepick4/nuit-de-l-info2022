export class Board {
    constructor(levelData) {
        this.levelData;
        this.board = [];
        this.initBoard();
    }

    initBoard() {
        //parse the datas from the boardData
    }

    initLevel() {
        //init the level
    }

    clearBoard() {
        //clear the board
        this.board = [];
    }
}
